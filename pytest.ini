[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Quiet, informative default output. Coverage is opt-in via `--cov`.
addopts =
    -q
    --strict-markers
    --tb=short

markers =
    integration: exercises several layers together rather than one unit
